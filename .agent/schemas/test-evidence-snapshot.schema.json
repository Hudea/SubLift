{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Test Evidence Fingerprint v1",
  "$comment": "Canonical manifest is formed from the directly declared test artifacts, sorted by byte-order path, as <role>\\t<path>\\t<content_sha256>\\n. The value is SHA-256 of the manifest bytes. The artifact list remains in the Testing result for explainability.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "algorithm", "value"],
  "properties": {
    "schema_version": {"const": 1},
    "algorithm": {"const": "sha256-artifact-manifest-v1"},
    "value": {"type": "string", "pattern": "^[a-f0-9]{64}$"}
  }
}
