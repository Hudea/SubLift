"""Isolated offline tools for SubLift (benchmark, scoring, frozen Oracle).

This package is not the product runtime. The product command is Native
``build/cpp/bin/sublift``. Optional Python tools live here so they do not
occupy the product ``sublift`` command or Native artifacts.
"""

from __future__ import annotations

__version__ = "0.1.0"
