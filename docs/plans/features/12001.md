# Feature 12001: 冻结 Phase 12 设计与实施基线

## 1. 目标

把 **Cross-Platform Web UI & Native Server** 登记为独立的 **Phase 12**：
1. 冻结 Web API、HTTP 206 视频流、SSE 实时事件流与 C++ 原生服务线程架构。
2. 冻结 Web 单视频工作台（对齐 Phase 10 Native Workbench）的交互与组件规划。
3. 建立 `docs/phases/phase12.json` 并在 `phases.json` 与 `progress.md` 中正确登记。
4. 拆解 `12101` ~ `12401` 的顺序实施 Feature 链条。

---

## 2. 交付与模块清单

| 文件 | 变更说明 |
|---|---|
| `docs/plans/architecture/phase12-cross-platform-web-ui.md` | 架构设计全貌与 API/SSE 契约 |
| `docs/plans/features/12001.md` | 本 Feature 规划文档 |
| `docs/phases/phase12.json` | Canonical Phase 12 跟踪详情 |
| `phases.json` | 注册 phase12 索引 |
| `progress.md` | 会话导航与状态更新 |

---

## 3. 验收标准 (Acceptance Criteria)

- [x] 架构计划清晰规定 C++ 原生服务（0 Python 依赖、0 IPC 内存调用）、HTTP 206 视频流与 SSE 实时事件流契约。
- [x] Phase 12 使用 `12001`~`12401` 五位 ID、canonical 结构、显式依赖，并通过 `docs/phases/phaseN.schema.json` 校验。
- [x] `phases.json` 正确引用 `docs/phases/phase12.json`，且 `./init.sh` 运行通过。
