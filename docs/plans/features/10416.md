# 10416 Phase 10 最终文档收口与合入准备

> 状态：done（2026-08-12）。计划正文；验收证据写入 `docs/phases/phase10.json` 的 10416.evidence。

## 目标

将 Phase 10 实施完成后的当前文档统一到 10415 最终产品状态，修正入口、需求、架构、GUI/UI
设计和 Feature 计划中的状态漂移；历史 evidence 保留当时事实。按用户明确要求，将 07002 的
Phase 7/原 Phase 9 台账迁移一并纳入同一次最终文档提交，为 `UI/Upgrade` 合入做准备。

## 验收重点

1. 当前文档不再称 Phase 10 待开发、界面尚未升级或仍使用旧 ContentView/SubtitleList 组合。
2. 当前测试数字统一引用最终 `201 XCTest + 140 Swift Testing`；历史 evidence 数字不改写。
3. Requirements 如实记录 V10/真实 VoiceOver/部分真实点击仍受系统权限限制，不伪造通过。
4. 所有 Feature 计划状态与 `phase10.json` 一致，架构计划补记 10414–10416。
5. 最终提交只包含文档、Phase/schema 导航和旧 Phase 容器迁移，不包含产品代码或构建产物。

## 验证

- `env -u PYTHONPATH swift test`（`apps/macos`）
- `env -u PYTHONPATH ./scripts/verify-standard.sh`
- Draft 2020-12 JSON Schema + Feature ID/依赖/状态核验
- Markdown 本地链接与当前状态漂移扫描
- `git diff --check`、`./init.sh`
- staged diff 范围与 commit message 审计

## 非目标

- 不新增或修改产品功能。
- 不补做需要系统权限的真实 VoiceOver、辅助功能设置或鼠标点击验收。
- 不创建新 Phase 9，不 push，不执行分支合并。
