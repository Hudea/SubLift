# 10412 响应式与辅助功能硬化 — 执行计划

> 状态：done（2026-08-12）。计划正文；验收证据写入 `docs/phases/phase10.json` 的 10412.evidence。

## 目标

在最终 UI 上重跑外观矩阵（V01–V10）、硬化键盘路径（A01）与 VoiceOver（A02）。

## 验收标准（来自 phase10.json）

1. 在最终实现上重跑 V01–V10 截图。
2. 完成 A01（键盘路径）/A02（VoiceOver）。
3. 检查外观和辅助功能矩阵（Light/Dark/系统 Accent/Increase Contrast/Reduce Transparency/Reduce Motion）。
4. 发现问题必须修复或如实记录。

## 修改文件

| 文件 | 动作 | 内容 |
|---|---|---|
| `apps/macos/Sources/SubLiftMac/App/KeyboardShortcutCatalog.swift` | 新增 | 快捷键纯数据目录（供测试与文档；含 Esc 退出区域编辑） |
| `apps/macos/Sources/SubLiftMac/UI/Workspace/WorkspaceRootView.swift` | 修改 | regionEditing 时 Esc 退出（onExitCommand） |
| `apps/macos/Tests/SubLiftMacTests/ResponsiveAccessibilityTests.swift` | 新增 | 快捷键目录无重复/关键动作存在；响应式布局回归 |

## 测试先行顺序（TDD）

1. KeyboardShortcutCatalogTests：无重复（key+modifiers 唯一）、关键动作（Open/Export/Split/Merge/Play/Region/Inspector/Esc 退出）存在。
2. 实现后专项通过 → 完整 swift test。

## 外观矩阵（Computer Use/EvidenceShot）

- Light：V01、V03、V06、V07 重跑（代表性状态：empty/ready/review/settings）。
- Dark：同组截图（Dark 重跑）。
- 系统 Accent：全部视图用 Color.accentColor / NSColor 系统语义（代码审查确认）。
- Increase Contrast / Reduce Transparency / Reduce Motion：系统组件自动适配（代码审查 + 记录无法真实验证项）。

## 截图证据 ID

- `docs/design_ui/evidence/10412/V01-welcome-light.png` 等（Light/Dark 矩阵重跑）

## 风险与停止条件

- 真实 VoiceOver/键盘事件受系统权限阻塞：A01/A02 以代码路径与测试为准，未执行项如实记录。
