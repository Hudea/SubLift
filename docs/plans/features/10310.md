# 10310 实现 Subtitle Timeline 与播放导航 — 执行计划

> 状态：in-progress（2026-08-12 启动）。计划正文；验收证据写入 `docs/phases/phase10.json` 的 10310.evidence。

## 目标

28–40pt 字幕时间条带，确定性时间几何，点击 seek，Previous/Next/Space 复用已有命令。

## 验收标准（来自 phase10.json）

1. Timeline 高 28–40pt；使用确定性时间几何。
2. 极短条目有最小命中宽度；点击 seek。
3. Previous/Next/Space 复用已有命令。
4. 不实现波形、多轨、缩放或拖拽改时。
5. 时间几何 tests、swift test 与 V06/截图写入 evidence。

## 修改文件

| 文件 | 动作 | 内容 |
|---|---|---|
| `apps/macos/Sources/SubLiftMac/UI/Timeline/TimelineGeometry.swift` | 新增 | 确定性时间几何纯逻辑（条目 → x 映射、最小命中宽度、播放头位置）+ TimelineNavigation（Previous/Next 纯逻辑） |
| `apps/macos/Sources/SubLiftMac/UI/Timeline/SubtitleTimelineView.swift` | 新增 | 28–40pt 条带（条目块 + 播放头 + 点击 seek + Previous/Next 按钮） |
| `apps/macos/Sources/SubLiftMac/UI/Transcript/TranscriptPanel.swift` | 修改 | header 下方接入 Timeline |
| `apps/macos/Tests/SubLiftMacTests/SubtitleTimelineTests.swift` | 新增 | 几何映射/最小宽度/导航测试 |

## 测试先行顺序（TDD）

1. TimelineGeometryTests：条目 x 映射确定性（起止/范围）、边界、极短条目最小命中宽度、播放头位置。
2. TimelineNavigationTests：Previous/Next 边界（当前在首条之前/末条之后/条目中间）。
3. 实现后专项通过 → 完整 swift test。

## 状态与数据所有权

- 时间映射为纯函数（rangeStart/rangeDuration/width 输入，x 输出）。
- seek/播放命令复用 PlayerModel 与 Workspace 命令（不新建播放逻辑）。

## Computer Use 验收

- Timeline 截图（review 状态，含条目块与播放头），Light。

## 截图证据 ID

- `docs/design_ui/evidence/10310/V06b-timeline.png`

## 风险与停止条件

- 不实现波形、多轨、缩放或拖拽改时（non-goals）。
