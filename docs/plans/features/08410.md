# 08410 完成 Phase 8 综合审核与验收 — 执行计划

> 状态：in-progress（2026-08-13 启动）。验收证据写入 `docs/phases/phase8.json` 的 08410.evidence。

## 综合验收矩阵（冻结）

### 1. 真实混合目录（验收 1）

- 临时目录构造：支持（mp4/mov）+ 无效（txt）+ 重复（同文件两次）+ 嵌套（子目录）+ 已有 SRT + 隐藏文件；
- 默认非递归与显式递归结果、拒绝摘要（接受/跳过/拒绝）、三冲突策略（skip/rename/replace）与合同一致；
- 用 BatchInputScanner + BatchOutputPlanner 集成测试（真实文件系统，非 fake）。

### 2. 真实串行闭环（验收 2）

- 至少 3 个实际任务串行：completed → failed（或 cancelled）→ 继续 → retry 回 waiting → 输出定位；
- 记录 Worker teardown 后下一项才启动（FakeRunner maxActive==1 已有；真实 runner 用 smoke 素材验证串行顺序）；
- 输出定位（Finder 存在性检查已有单测）。

### 3. 恢复与资源（验收 3）

- 应用恢复：active → interrupted 且不自动启动（08207 测试已有——补 100 tasks 恢复时间/体积记录）；
- 损坏清单 fail-closed（08207 已有）；
- 100 Fake tasks：清单体积/恢复时间 + 运行后内存/残留 Worker 检查（进程级：真实运行后 ps 检查无残留 worker）。

### 4. 独立审核（验收 4）

- 3 路并行只读审核（Domain/调度与取消；路径/持久化安全；SwiftUI/a11y/测试证据）；
- 所有 P0/P1/P2 修复 + 原方向复核，无阻断项。

### 5. 证据与项目门（验收 5）

- B01–B10 证据索引（08308 的 B01/B03/B04/B05/B06/B07/B10 + 08309 的 B02/B08/B09——合并 08410 evidence 索引清单）；
- 1280/960、Light/Dark/Accent 齐全；
- 项目门：完整 swift test、verify-standard、JSON Schema（结构级核验替代——jsonschema 系统 python3 缺失）、依赖/状态核验、Markdown 链接、diff-check、init.sh。

### 文档收口

- docs/REQUIREMENTS.md、docs/ARCHITECTURE.md、docs/DECISIONS.md、docs/design/macos-gui.md、docs/design_ui/batch-task-center.md、docs/plans/architecture/phase8-batch-task-center.md 同步 Phase 8 完成状态；
- phases.json phase8 → done；progress.md 更新。

## 允许文件

- `apps/macos/Sources/SubLiftMac/`、`apps/macos/Tests/SubLiftMacTests/`（仅综合验收测试）
- `docs/design_ui/evidence/08410/`、`docs/plans/features/08410.md`、文档收口文件

## 停止条件

- 不实现并行/监听/新引擎/数据库/分发/Worker 改造；不以 fake/DEBUG fixture 冒充真实 IPC/文件写入/取消恢复/文件夹组合验收。
