# 10311 实现 Settings 与引擎可见性 — 执行计划

> 状态：in-progress（2026-08-12 启动）。计划正文；验收证据写入 `docs/phases/phase10.json` 的 10311.evidence。

## 目标

Settings 三分区（General/Recognition/Advanced），引擎与 capability 的真实可见性。

## 验收标准（来自 phase10.json）

1. Settings 分为 General、Recognition、Advanced。
2. Automatic 和 Whisper 不出现。
3. Mock 只在 Developer Mode。
4. Python 只作为显式 Oracle/回滚。
5. capability 缺失用真实 inline 状态。
6. 不提供假模型下载按钮。
7. 生成 V07 截图。

## 修改文件

| 文件 | 动作 | 内容 |
|---|---|---|
| `apps/macos/Sources/SubLiftMac/UI/Settings/EngineCapability.swift` | 新增 | capability 纯逻辑：visibleEngines（mock 仅 developer）、status（ffmpeg/paddle 注入）、runtimeLabel（RuntimePolicy.resolve 真实值） |
| `apps/macos/Sources/SubLiftMac/UI/Settings/SettingsView.swift` | 重写 | TabView 三分区：General（采样质量）、Recognition（引擎 picker + 真实 capability inline）、Advanced（Developer Mode、runtime 可见性；无假下载按钮） |
| `apps/macos/Tests/SubLiftMacTests/SettingsTests.swift` | 新增 | capability/分区/无 Automatic/无下载按钮测试 |

## 测试先行顺序（TDD）

1. EngineCapabilityTests：visibleEngines（mock 仅 developer）、status（ffmpeg/paddle/developer 注入）、runtimeLabel（cpp/python oracle）。
2. SettingsPresentationTests：分区列表、无 Automatic/Whisper、无假下载按钮（静态断言）。
3. 实现后专项通过 → 完整 swift test。

## 状态与数据所有权

- @AppStorage（default_engine/sampling_quality/developer_mode）跨 Session 偏好；capability 检测为真实系统状态（FfmpegDetector / 模型目录存在性）。
- Python 仅显式 Oracle/回滚（RuntimePolicy.resolve 语义不变，只展示）。

## Computer Use 验收

- V07：Settings 面板截图（General/Recognition/Advanced），Light。

## 截图证据 ID

- `docs/design_ui/evidence/10311/V07-settings.png`

## 风险与停止条件

- 不提供假模型下载按钮；不新增第三方依赖。
